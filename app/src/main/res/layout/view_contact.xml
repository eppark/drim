<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingLeft="8dp"
    android:paddingStart="8dp"
    android:paddingEnd="8dp"
    android:paddingTop="8dp"
    android:paddingRight="8dp">

    <TextView
        android:id="@+id/tvName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:fontFamily="@font/sspbold"
        android:maxLines="1"
        android:singleLine="true"
        android:textSize="18sp"
        tools:text="Kyoko Kirigirigirigirigirigirigirigirigirigirigirigirigirigirigirigirigi" />

    <TextView
        android:id="@+id/tvNumber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tvName"
        android:layout_marginTop="4dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:layout_toStartOf="@id/tvType"
        android:layout_alignParentStart="true"
        tools:text="1103711037"
        android:layout_toLeftOf="@id/tvType"
        android:layout_alignParentLeft="true" />

    <TextView
        android:id="@+id/tvType"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tvName"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_marginTop="4dp"
        android:ellipsize="end"
        android:maxLines="1"
        tools:text="Mobile" />

    <View
        android:id="@+id/divider3"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_below="@+id/tvNumber"
        android:layout_marginTop="8dp"
        android:background="#80888A" />

</RelativeLayout>